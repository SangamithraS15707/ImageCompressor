# Image Compression using K-Means Clustering

This project is a **Streamlit web app** that compresses images using **K-Means clustering**.  
It automatically determines the optimal number of clusters (K) using the **Elbow Method** with `KneeLocator`.

---

## 🚀 Features
- Upload `.jpg`, `.jpeg` images.
- Automatically detect the optimal number of clusters (K).
- Compress the image using K-Means clustering.
- Display both the original and compressed images.
- Download the compressed image.

